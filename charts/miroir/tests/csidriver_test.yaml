suite: test csidriver
templates:
  - csidriver.tpl
release:
  name: miroir
  namespace: miroir-system
tests:
  - it: renders a single CSIDriver named miroir.home-operations.com with the expected capabilities
    asserts:
      - hasDocuments:
          count: 1
      - containsDocument:
          kind: CSIDriver
          name: miroir.home-operations.com
          apiVersion: storage.k8s.io/v1
      - equal:
          path: spec.attachRequired
          value: false
      - equal:
          path: spec.podInfoOnMount
          value: false
      - equal:
          path: spec.fsGroupPolicy
          value: ReadWriteOnceWithFSType
      - equal:
          path: spec.storageCapacity
          value: false
      - contains:
          path: spec.volumeLifecycleModes
          content: Persistent
  - it: flips storageCapacity when the capacity feature is enabled
    set:
      storageCapacity:
        enabled: true
    asserts:
      - equal:
          path: spec.storageCapacity
          value: true
