miroir-config {{ .Chart.Version }} rendered:

  MiroirNodes:           {{ len (.Values.nodes | default dict) }}
  MiroirNodeGroups:      {{ len (.Values.nodeGroups | default dict) }}
  StorageClasses:        {{ len (.Values.storageClasses | default list) }}
  VolumeSnapshotClasses: {{ len (.Values.volumeSnapshotClasses | default list) }}
{{- if and (not .Values.nodes) (not .Values.nodeGroups) (not .Values.storageClasses) (not .Values.volumeSnapshotClasses) }}

Nothing was rendered: every collection is empty. If your values file sets
something, check the top-level key spelling (nodes, nodeGroups,
storageClasses, volumeSnapshotClasses) — an unrecognized top-level key is
silently ignored.
{{- end }}

Inspect the topology:

  kubectl get miroirnodes,miroirnodegroups
