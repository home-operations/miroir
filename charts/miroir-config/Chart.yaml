apiVersion: v2
name: miroir-config
description: Storage configuration for the miroir CSI driver — node topology (MiroirNodes), StorageClasses, and VolumeSnapshotClasses as one reviewed document
type: application
# Placeholder — CI overrides both at publish time (helm package
# --version/--app-version from the release tag).
version: 0.0.1
appVersion: 0.0.1
kubeVersion: ">=1.31.0"
home: https://github.com/home-operations/miroir
sources:
  - https://github.com/home-operations/miroir
maintainers:
  - name: home-operations
    email: contact@home-operations.com
keywords:
  - storage
  - kubernetes
annotations:
  category: Infrastructure
